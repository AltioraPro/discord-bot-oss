/**
 * The colour every embed the bot sends is accented with.
 *
 * It lives here rather than beside one feature's messages so deepwork and role
 * sync cannot drift apart: two copies of a brand colour is two chances to
 * change only one of them.
 */
export const ACCENT = 0x58_65_f2;
